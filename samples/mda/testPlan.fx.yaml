testSuite:
  testSuiteName: MDA tests
  testSuiteDescription: Verify model driven application
  persona: User1
  appLogicalName: new_buttonclicker_0a877
  onTestSuiteComplete: Screenshot("pause_onTestSuiteComplete.png");

  testCases:
    - testCaseName: Verify
      testCaseDescription: Pause example
      testSteps: |
        = Assert(cat_name.Text = "Machine Requests", "Name text");
          Assert(cat_name.Label = "Name", "Name Label")

testSettings:
  headless: false
  locale: "en-US"
  recordVideo: true
  extensionModules:
    enable: true
  browserConfigurations:
    - browser: Chromium

environmentVariables:
  users:
    - personaName: User1
      emailKey: NotNeeded
      passwordKey: NotNeeded
