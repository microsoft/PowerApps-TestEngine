testSuite:
  testSuiteName: Copilot Studio Kit
  testSuiteDescription: component List of Copilot Studio Kit 
  persona: User1
  appLogicalName: NotNeeded
  onTestSuiteStart: |
    = If(
        CountRows(bot_component) > 0, 
        ForAll(bot_component, Remove(bot_component,ThisRecord))
      );
      Collect(bot_component,{
        cat_name:"TestComponent1"
      });
  testCases:
    - testCaseName: Step 1 - Start the entity details
      testCaseDescription: Verify can open the page
      testSteps: |
        = 
        Assert(1=1) 
      
testSettings:
  filePath: ./testSettings.yaml

environmentVariables:
  users:
    - personaName: User1
      emailKey: user1Email
      passwordKey: NotNeeded
